name: Build .deb

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v5

      - name: Setup Theos & tools
        run: |
          brew install pkg-config openssl md5sha1sum dpkg perl
          cd $HOME
          git clone --depth=1 https://github.com/theos/theos.git
          # 补 dm.pl（关键！）
          curl -L -o theos/bin/dm.pl https://raw.githubusercontent.com/theos/dm.pl/master/dm.pl
          chmod +x theos/bin/dm.pl
          # ldid
          mkdir -p theos/bin
          curl -L -o theos/bin/ldid https://github.com/ProcursusTeam/ldid/releases/latest/download/ldid_macosx_x86_64
          chmod +x theos/bin/ldid

      - name: Build
        env:
          THEOS: /Users/runner/theos
        run: |
          TWEAK_DIR=$(find . -name "Tweak.x" -exec dirname {} \; | head -1)
          echo "TWEAK_DIR = $TWEAK_DIR"
          cd "$TWEAK_DIR"
          make clean package 2>&1 | tee build.log

      - name: Find .deb
        run: |
          find . -name "*.deb"

      - name: Upload .deb
        uses: actions/upload-artifact@v4
        with:
          name: CookieHook
          path: "**/*.deb"
